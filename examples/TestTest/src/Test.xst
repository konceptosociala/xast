module Test exports *

extern type Int;
extern fn opAdd(Int, Int) -> Int;

@Mode(Strict)
system Hello #(Int) -> Int;
system Hello 100   = 0;
system Hello other = other + 1;

-- void Test_Hello(ecs_iter* it) {
--     int32_t* _b0 = ecs_field(it, int32_t, 0);
--     if (_b0 == 100) {
--         {
--             _b0 = 0;
--         }
--     } else {
--         {
--             int32_t t0 = opAdd(_b0, 1);
--             _b0 = t0;
--         }
--     }
--     return;
-- }