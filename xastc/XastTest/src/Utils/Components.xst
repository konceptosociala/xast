module Utils.Components exports { Pos, Velocity, Player }

use Main *

type Pos = Pos {
   x : Int,
   y : Int
};

type Velocity = Velocity Int Int;

type Player = Player;

type UnexportedType = UnexportedType;