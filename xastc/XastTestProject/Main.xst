module Main exports { UpdatePlayerPosition }

use Components *

system UpdatePlayerPosition (Player, Pos, Velocity) 
   -> (Position, List Effect);

system UpdatePlayerPosition p pos (Velocity velX velY) = (pos, Event (p, pos));
